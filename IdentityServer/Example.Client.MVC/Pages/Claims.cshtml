@page
@model ClaimsModel
<h1>Hello, world!</h1>
<h2>The time on the server is @DateTime.Now</h2>

<h3>Claims</h3>
<dl>
  @foreach (var claim in User.Claims)
  {
    <dt>@claim.Type</dt>
    <dd>@claim.Value</dd>
  }
</dl>

<a href="logout">Logout</a>